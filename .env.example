# KrishiAI Backend - External Secrets Template
# Only declare variables that require environment-level secret injection.
# Avoid putting local database credentials or default URLs here to prevent overriding application.properties.

# OpenWeather API Secret Key (Keep this secret!)
OPENWEATHER_API_KEY=
